/**
 * 2 bit multiplexor. If sel==1 then out=b else out=a.
 */

CHIP Mux2 {
    IN a[2], b[2], sel;
    OUT out[2];

    PARTS:
    Not(in=sel,out=notsel);
    
    Mux(a=a[0],b=b[0],sel=sel,out=out[0]);
    Mux(a=a[1],b=b[1],sel=sel,out=out[1]);
}

